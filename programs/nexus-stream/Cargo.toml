[package]
name = "nexus-stream"
version = "0.1.0"
description = "NexusStream — tips, parimutuel predictions, milestone ledger"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "nexus_stream"

[features]
no-entrypoint = []
no-idl = []
no-log-ix-name = []
cpi = ["no-entrypoint"]
default = []

[dependencies]
anchor-lang = "0.30.1"
